name: Java CI/CD with Gradle

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    # 1. GitHub 코드 가져오기
    - name: Checkout source code
      uses: actions/checkout@v4

    # 2. JDK 설정
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    # 3. Gradle 설정
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    # 4. gradlew 실행 권한 부여
    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew

    # 5. 프로젝트 빌드
    - name: Build with Gradle
      run: ./gradlew build

    # 6. 빌드된 JAR 서버로 전송
    - name: Copy JAR to remote server
      run: |
        scp -i private_key.pem -o StrictHostKeyChecking=no -P ${{ secrets.PORT }} ./build/libs/*.jar ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/home/ec2-user/cicd/

    # 7. deploy.sh 서버로 전송
    - name: Copy deploy.sh to remote server
      run: |
        scp -i private_key.pem -o StrictHostKeyChecking=no -P ${{ secrets.PORT }} ./deploy.sh ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/home/ec2-user/cicd/

    # 8. SSH 접속 후 deploy.sh 실행
    - name: Run deploy script on remote server
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.PRIVATE_KEY }}
        port: ${{ secrets.PORT }}
        script: |
          chmod +x /home/ec2-user/cicd/deploy.sh
          /home/ec2-user/cicd/deploy.sh
